package e.numbers;

public class I_Ceil {
    public static void main(String[] args) {
        // rounds the specified double value upward and returns it.
        double x = -40.985;
        float y = -20;
        System.out.println(Math.ceil(x));
        System.out.println(Math.ceil(y));
    }
}
